spring.application.name=Interview-Platform
server.port=${PORT:8080}


spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/withsecurity}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:root123}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

jwt.secret=my-secret-key-my-secret-key-my-secret-key-123456
# When deploying on Railway, set this environment variable (UPLOAD_DIR) to point to your Railway Volume path, e.g., /app/uploads/
file.upload.dir=${UPLOAD_DIR:uploads/}
# jwt.access.expiration=900000
# jwt.refresh.expiration=604800000

# SMTP configuration for password reset OTP emails
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=samiyaajijshaikh@gmail.com
spring.mail.password=bjii lnpp qpwf wsjg
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
# Optional override (defaults to spring.mail.username)
# app.mail.from=no-reply@yourdomain.com
# ── Video upload size limits (videos can be large) ──
spring.servlet.multipart.max-file-size=500MB
spring.servlet.multipart.max-request-size=500MB